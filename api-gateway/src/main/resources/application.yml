server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
              - "http://localhost:3000"
              - "https://dev-ops-cloud-sand-box.vercel.app"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
              - PATCH
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/v1/auth/**, /auth/v3/api-docs/**
          # StripPrefix removed — auth-service needs the full /api/v1/auth prefix

        - id: container-sim-service-api
          uri: lb://container-sim-service
          predicates:
            - Path=/api/v1/containers/**
          filters:
            - AuthenticationFilter

        - id: container-sim-service-docs
          uri: lb://container-sim-service
          predicates:
            - Path=/containers/v3/api-docs/**
          filters:
            - StripPrefix=1

        - id: progress-service
          uri: lb://progress-service
          predicates:
            - Path=/api/v1/progress/**
          filters:
            - AuthenticationFilter

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: Authentication Service
        url: /auth/v3/api-docs
      - name: Container Simulator Service
        url: /containers/v3/api-docs
  api-docs:
    path: /v3/api-docs

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}

jwt:
  secret: ${JWT_SECRET:1zcbHpUZkLDels67-SUEGSSt_nWdQpc_2hamlL5LJwa}
